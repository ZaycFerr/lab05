#ifndef PRINT_HPP
#define PRINT_HPP

#include <iostream>
#include <fstream>
#include <string>

void print(const std::string& text, std::ostream& out);
void print(const std::string& text, std::ofstream& out);

#endif
